import Button from '@/components/MDButton';
import MuiButton from '@mui/material/Button';
import Box from '@mui/material/Box';
export default function HomePage() {
   return (
      <div>
         <MuiButton size="small" color="primary" variant="text">
            MUI Button
         </MuiButton>
      </div>
   );
}
